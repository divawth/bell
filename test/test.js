require('../dist/bell');

describe('我的测试', function () {
  describe('bell', function () {
    it('run start', function () {
      // 示例化 Vue, 此时示例对应的 DOM 元素没有在页面中。
      console.log(21)
    })
  })
})